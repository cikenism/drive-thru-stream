services:
  - type: web
    name: drive-thru-backend
    runtime: python
    buildCommand: ""
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: GEMINI_API_KEY
        value: "ISI_API_KEY_KAMU"
